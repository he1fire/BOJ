#include <bits/stdc++.h>
#define ll long long
using namespace std;
typedef pair<ll,ll> pll;
ll N, M, T;
int main(){
    scanf("%lld + %lld = %lld", &N, &M, &T);
    N+M==T ? cout << "YES" : cout << "NO";
    return 0;
}