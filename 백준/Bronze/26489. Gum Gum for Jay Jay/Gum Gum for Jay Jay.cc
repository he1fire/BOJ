#include <bits/stdc++.h>
#define ll long long
using namespace std;
string S;
ll ans;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    while (getline(cin,S))
        ans++;
    cout << ans;
    return 0;
}